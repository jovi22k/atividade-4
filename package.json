{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon src/index.js",
    "build": "tsc -p . || echo 'no build step'",
    "test": "jest --runInBand",
    "lint": "eslint . --ext .js,.ts",
    "migrate": "node-pg-migrate up",
    "migrate:down": "node-pg-migrate down"
  },
  "dependencies": {
    "pg": "^8.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.0.0",
    "node-pg-migrate": "^6.0.0",
    "nodemon": "^2.0.0"
  }
}